apiVersion: metallb.io/v1beta1
kind: IPAddressPool
metadata:
  name: app-pool
  namespace: metallb
spec:
  addresses:
  - 10.6.64.66-10.6.64.126
  autoAssign: false
  avoidBuggyIPs: true
  serviceAllocation:
    serviceSelectors:
      - matchExpressions:
        - { key: audience, operator: In, values: [public] }
